# Copy to .env (gitignored). Never commit real values.
# Generate secrets locally; do not reuse production keys.

COMPOSE_PROJECT_NAME=pao-amcp-2096
FRONTEND_PORT=13000
BACKEND_PORT=14000
POSTGRES_PASSWORD=change-me
JWT_SECRET=change-me-min-32-characters-please
ENCRYPTION_KEY=change-me-32-characters-exact!!
MCP_BEARER_TOKEN=change-me-mcp-bearer-token

# Used by provision + live tests (not passed into the container)
PAO_ANYTHINGMCP_ADMIN_EMAIL=pao-amcp-admin@example.com
PAO_ANYTHINGMCP_ADMIN_PASSWORD=Change-me-admin-9!
